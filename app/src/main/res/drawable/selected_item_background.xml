<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Màu cho item được chọn -->
    <item android:drawable="@color/blue_200" android:state_checked="true"/>
    <!-- Màu mặc định -->
    <item android:drawable="@android:color/transparent"/>
</selector>
